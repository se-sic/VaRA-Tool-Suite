sudo: false
dist: trusty
language: python
python:
  - "3.5"
  - "3.6"

addons:
  apt:
    packages:
      - time
      - libgit2-dev
      - ninja

install:
  - pip install .
  # - pip install -r requirements.txt
  - pip install codecov
  - pip install pytest-cov

script:
  - pytest --cov=./ varats tests
  - codecov

branches:
  only:
    - vara-dev
